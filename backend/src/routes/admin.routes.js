const express = require('express');
const router = express.Router();

// Stub for admin routes added during merge conflict resolution
// Ensure to pull the actual admin.routes.js from the main branch.

module.exports = router;
